# Broker MQTT locale per sviluppo/demo. Nessuna autenticazione (allow_anonymous)
# perche' AlertNotifier si connette senza credenziali (mqtt.Client() senza
# username_pw_set). Solo per uso locale/demo, non esporre cosi' in produzione.
listener 1883
allow_anonymous true
